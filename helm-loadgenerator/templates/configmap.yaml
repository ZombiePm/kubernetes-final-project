apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "loadgenerator.fullname" . }}-env
  labels:
    {{- include "loadgenerator.labels" . | nindent 4 }}
data:
  {{- range $key, $value := .Values.env }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}